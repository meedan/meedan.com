<!doctype html>
<html lang="<%= t(:locale) %>">

<%= partial "partials/head" %>

<body class="<%= page_classes %> <%= current_page.data.slug %>">
	<div style="height: 0; width: 0; position: absolute; visibility: hidden">
		<%= partial "images/vector/vector.svg" %>
	</div>

  <% unless current_page.data.slug == "bridge-privacy" %>
    <%= partial "partials/bootstrap-nav.html" %>
  <% end %>

	<main>
		<%= yield %>
	</main>

  <%= javascript_include_tag "app" %>
  <%= partial "partials/ga" %>

	</body>
</html>